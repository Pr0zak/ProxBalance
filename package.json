{
  "name": "proxbalance",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bash build.sh"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "esbuild": "^0.20.0"
  }
}
